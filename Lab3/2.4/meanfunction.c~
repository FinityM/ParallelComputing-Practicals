float meanFunction(float array[], int am);
float meanFunction(float array[], int am){
    float sum = 0;
    float mean;
    int i;

    for(i = 0; i < am; i++){
        sum += array[i];
    }
    mean = (float) sum / am;

    return mean;
}

